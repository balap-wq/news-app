name: Playwright Smoke Tests

on:
  push:
    branches:
      - main
      - master
      - feature/**

  pull_request:
    branches:
      - main
      - master
      - feature/**

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: articles
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d articles"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DB_HOST: localhost
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: articles
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/articles

      PORT: 5000
      FRONTEND_URL: http://localhost:5173
      CORS_ORIGIN: http://localhost:5173
      NODE_ENV: development

      NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
      NEWS_API_BASE_URL: https://newsapi.org/v2/

      NEW_RELIC_ENABLED: false
      NEW_RELIC_NO_CONFIG_FILE: true
      NEW_RELIC_LICENSE_KEY: dummy
      NODE_OPTIONS: ""

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          # Cache all three lockfiles so repeated runs skip re-downloading packages
          cache: "npm"
          cache-dependency-path: |
            package-lock.json
            backend/package-lock.json
            frontend/package-lock.json

      # ── Root ─────────────────────────────────────────────────────────────────
      - name: Install Root Dependencies
        run: npm ci

      # ── Backend ──────────────────────────────────────────────────────────────
      - name: Install Backend Dependencies
        run: npm ci --prefix backend

      - name: Generate Prisma Client
        # Must run BEFORE the server starts (Playwright webServer config boots it).
        # Without this step the import of PrismaClient throws at startup.
        run: npx prisma generate --schema=backend/prisma/schema.prisma

      - name: Run Database Migrations
        # Applies all pending migrations to the CI postgres instance.
        run: npx prisma migrate deploy --schema=backend/prisma/schema.prisma

      # ── Frontend ─────────────────────────────────────────────────────────────
      - name: Install Frontend Dependencies
        run: npm ci --prefix frontend

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # ── Tests ─────────────────────────────────────────────────────────────────
      - name: Run Playwright Tests
        run: npx playwright test

      # ── Artifacts ────────────────────────────────────────────────────────────
      - name: Upload HTML Report
        uses: actions/upload-artifact@v4
        if: always()  # Upload even when tests fail so you can debug
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30